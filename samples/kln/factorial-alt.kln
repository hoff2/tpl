// Factorial function!
// This one counts up instead of down
// Based on program by Ivan and Andrew

factorial( cur:integer, max:integer ) : integer
  if cur<max then//Already reached goal
    factorial(cur+1, max) * cur
  else
    max
  endif


// Main Procedure
main( x : integer ) : integer
  print( factorial(x, 1) ) //Give the 1 as a starting state for the calculation
  true